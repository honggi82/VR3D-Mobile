# VR3D Mobile Implementation Plan

1. Define package/API contracts and implement testable PC security, storage, rendering, and
   job orchestration modules.
2. Add the FastAPI surface and Tkinter operations GUI with fail-closed health state.
3. Build the bilingual static GitHub Pages client and exercise it against the local API.
4. Build the Kotlin Android app with package import, library, sensor calibration, and smooth
   adjacent-view interpolation.
5. Run offline tests, then real depth, browser, Gradle/APK, and available device checks.
6. Record every deviation in `IMPLEMENTATION_NOTES.md`; do not publish until the user confirms.
